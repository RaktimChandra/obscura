# Optional configuration. Copy to .env or set in your shell.
# Serve the built frontend from FastAPI (set automatically in Docker):
OBSCURA_SERVE_FRONTEND=0
# Real video path: absolute path to a clip, or a webcam index like 0
# OBSCURA_VIDEO=/path/to/clip.mp4
